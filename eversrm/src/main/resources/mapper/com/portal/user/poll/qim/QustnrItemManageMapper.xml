<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.portal.user.poll.qim.QustnrItemManageMapper">

	<!-- 설문조사(설문결과) 삭제 -->
	<delete id="deleteQustnrRespondInfo" parameterType="map">
		DELETE FROM LETTNQUSTNRRSPNSRESULT 
		WHERE QUSTNR_IEM_ID = #{qustnrIemId}
	</delete>

	<!-- 설문정보::삭제  -->
	<delete id="deleteQustnrItemManage" parameterType="map">
		DELETE FROM LETTNQUSTNRIEM
		WHERE QUSTNR_IEM_ID = #{qustnrIemId}
	</delete>

	<!-- 설문정보::수정  -->
	<update id="updateQustnrItemManage" parameterType="map">
		UPDATE LETTNQUSTNRIEM
		SET
			IEM_SN=#{iemSn},
			IEM_CN=#{iemCn},
			ETC_ANSWER_AT=#{etcAnswerAt},
			LAST_UPDT_PNTTM = sysdate(),
			LAST_UPDUSR_ID = #{lastUpdusrId}
		WHERE QUSTNR_IEM_ID = #{qustnrIemId}
	</update>

	<!-- 설문정보::상세보기 -->
	<select id="selectQustnrItemManageDetail" parameterType="map" resultType="egovMap">
		SELECT
			A.QUSTNR_QESITM_ID		qestnrQesitmId,
			(SELECT QESTN_CN FROM LETTNQUSTNRQESITM WHERE QUSTNR_QESITM_ID = A.QUSTNR_QESITM_ID) QESTNR_QESITM_CN,
			A.QESTNR_ID,
			(SELECT QUSTNR_SJ FROM LETTNQESTNRINFO WHERE QESTNR_ID = A.QESTNR_ID) QESTNR_CN,
			A.IEM_SN,
			A.QUSTNR_IEM_ID,
			A.IEM_CN,
			A.ETC_ANSWER_AT,
			A.QUSTNR_TMPLAT_ID			qestnrTmplatId,
			DATE_FORMAT(A.FRST_REGIST_PNTTM, '%Y-%m-%d') as FRST_REGIST_PNTTM,
			A.FRST_REGISTER_ID,
			DATE_FORMAT(A.LAST_UPDT_PNTTM,'%Y-%m-%d') as LAST_UPDT_PNTTM,
			A.LAST_UPDUSR_ID
		FROM LETTNQUSTNRIEM A
		WHERE 1=1
		AND A.QUSTNR_IEM_ID = #{qustnrIemId}
	</select>

	<!-- 설문정보::목록조회_게시물정보 -->
	<select id="selectQustnrItemManageList" parameterType="map" resultType="egovMap">
		SELECT
			A.QUSTNR_QESITM_ID		qestnrQesitmId,
			A.QESTNR_ID,
			A.IEM_SN,
			A.QUSTNR_IEM_ID,
			A.IEM_CN,
			A.ETC_ANSWER_AT,
			A.QUSTNR_TMPLAT_ID		qestnrTmplatId,
			DATE_FORMAT(A.FRST_REGIST_PNTTM, '%Y-%m-%d') as FRST_REGIST_PNTTM,
			A.FRST_REGISTER_ID,
			(SELECT USER_NM FROM COMVNUSERMASTER WHERE ESNTL_ID = A.FRST_REGISTER_ID) FRST_REGISTER_NM,
			DATE_FORMAT(A.LAST_UPDT_PNTTM,'%Y-%m-%d') as LAST_UPDT_PNTTM,
			A.LAST_UPDUSR_ID
		FROM LETTNQUSTNRIEM A
		WHERE 1=1
	 <if test="searchKeyword != null and searchKeyword != ''">
            <if test="searchCondition == 'IEM_CN'">
                AND IEM_CN LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
		 	<if test="searchCondition == 'FRST_REGISTER_ID'">
		 		AND  FRST_REGISTER_ID IN (SELECT ESNTL_ID FROM COMVNUSERMASTER WHERE USER_NM LIKE CONCAT('%', #{searchKeyword}, '%') )
		 	</if>
	 </if>
		ORDER BY A.FRST_REGIST_PNTTM DESC
		LIMIT #{recordCountPerPage} OFFSET #{firstIndex}
	</select>

	<!-- 설문정보::목록조회_게시물 총갯수  -->
	<select id="selectQustnrItemManageListCnt" parameterType="map" resultType="int">
		SELECT
		COUNT(*) totcnt
		FROM LETTNQUSTNRIEM
		WHERE 1=1
	 <if test="searchKeyword != null and searchKeyword != ''">
            <if test="searchCondition == 'IEM_CN'">
                AND IEM_CN LIKE CONCAT('%', #{searchKeyword}, '%')
            </if>
	 	<if test="searchCondition == 'FRST_REGISTER_ID'">
	 		AND  FRST_REGISTER_ID IN (SELECT ESNTL_ID FROM COMVNUSERMASTER WHERE USER_NM LIKE CONCAT('%', #{searchKeyword}, '%') )
	 	</if>
	 </if>
	</select>

	<!-- 설문정보::입력  -->
	<insert id="insertQustnrItemManage" parameterType="map">
		INSERT INTO LETTNQUSTNRIEM (
			QUSTNR_QESITM_ID,
			QESTNR_ID,
			IEM_SN,
			QUSTNR_IEM_ID,
			IEM_CN,
			ETC_ANSWER_AT,
			QUSTNR_TMPLAT_ID,
			FRST_REGIST_PNTTM,
			FRST_REGISTER_ID,
			LAST_UPDT_PNTTM,
			LAST_UPDUSR_ID
		)VALUES(
			#{qestnrQesitmId},
			#{qestnrId},
			#{iemSn},
			#{qustnrIemId},
			#{iemCn},
			#{etcAnswerAt},
			#{qestnrTmplatId},
			sysdate(),
			#{frstRegisterId},
			sysdate(),
			#{lastUpdusrId}
		)
	</insert>

</mapper>
