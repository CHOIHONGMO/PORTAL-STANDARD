{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "Tomcat: Start and Attach (Port 8000)",
            "request": "attach",
            "hostName": "localhost",
            "port": 8000,
            "preLaunchTask": "Start Tomcat in Debug Mode"
        }
    ]
}
